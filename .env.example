# Required — your OpenAI API key. Keep this server-side only; never expose it to the browser.
OPENAI_API_KEY=your_openai_api_key_here

# Optional — defaults to gpt-4o-mini if not set.
OPENAI_MODEL=gpt-4o-mini
